{
  "name": "looperdo-root",
  "scripts": {
    "build": "cd looperdo_website/nextjs_space && rm -rf .next && npm install && npm run build",
    "start": "cd looperdo_website/nextjs_space && npm run start"
  },
  "dependencies": {
    "nodemailer": "^8.0.5",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "@types/uuid": "^10.0.0"
  }
}